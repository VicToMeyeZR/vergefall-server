# Copy to .env on the VPS and change the passwords.
# Do not use @ : / # in POSTGRES_PASSWORD — it is spliced into Nakama's address string.
DOMAIN=server1.trystarbot.space
CADDY_EMAIL=admin@trystarbot.space
POSTGRES_PASSWORD=changeMeNow
CONSOLE_PASSWORD=changeMeNow
SESSION_TOKEN_EXPIRY_SEC=7200
NAKAMA_LOG_LEVEL=INFO
